#include <stdio.h>
#include <stdlib.h>

void temp_for_AST(int b, int k, char* l[])
{
    *x->anyfld.some_field_sub = (float)&BB_and_B - sizeof(funktione(b + 2 * scanf(X_IS_SMTH)));
    random_function_call(arg1, arg2, arg3, arg4+arg5-arg6*arg7/arg8.b, c((double)sizeof(*structure->b)));

    switch(FYHKZU9(a + b + c + d + e + f))
    {
    case 1:
        {
            random_function_call(arg1, arg2, arg3, arg4+arg5-arg6*arg7/arg8.b, c((double)sizeof(*structure->b)));
            random_variable = 100 / sizeof(SMALL) + func_f(func_F(sizeof((float[])T)));
        }
        break;
    case 2:
        *z->b.g = (float*)&b - sizeof(funk(b - 256/printf(XXXXX)));
        break;
    default:
        {
            rndfnccll();
        }
        fncrndcll();
        {
            *x->b.OV = (float)&b - sizeof(fnk(UUU + 2*scanf(x)));
        }
        break;
    }

    switch(a)
        case 23:
            some_B = 1;
}
