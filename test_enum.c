#include <stdio.h>
#include <stdlib.h>

void temp_for_AST(int b_var, int k_var, char* l_var[])
{
    const static volatile enum ENUM_1 {RANDOM_1, RANDOM_2, RANDOM_3, RANDOM_4 = 1 + VAR_1 + sizeof(const volatile int) + sizeof(ID_1 + 200.1) + sizeof(0.5 + 1) + (float)(long)(int)(ID_2 + 1.5  + printf("%s", ID_3))/ID_4};
    const static volatile enum ENUM_1 VAR_2 = RANDOM;
    const volatile static enum ENUM_2 {a,b,c,d,r,f} VAR_3 = a;
    const volatile static enum {q,w,e,r,t,y,u} VAR_4[static const volatile b = 2] = ID_5;

    char b[10];
    b[123] += kkk;

    return xx + 0;
}
